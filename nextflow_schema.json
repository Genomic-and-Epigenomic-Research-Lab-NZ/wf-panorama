{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/./master/nextflow_schema.json",
    "title": "wf-panorama",
    "workflow_title": "Panorama sample processing workflow",
    "description": "Nextflow workflow for Panorama sample processing.",
    "url": "https://github.com/lucy924/wf-panorama",
    "type": "object",
    "definitions": {
        "input": {
            "title": "Input Options",
            "type": "object",
            "fa_icon": "fas fa-arrow-right",
            "description": "Parameters for finding and handling input data for analysis.",
            "properties": {
                "sample": {
                    "type": "string",
                    "title": "Sample name",
                    "description": "A single sample name.",
                    "help_text": "This is used to specify a single sample name of your choosing."
                },
                "bam_directory": {
                    "type": "string",
                    "format": "path",
                    "description": "BAM or unaligned BAM (uBAM) files to use in the analysis.",
                    "help_text": "This accepts the path to a folder containing BAM files."
                },
                "panel_metadata": {
                    "type": "string",
                    "format": "path",
                    "default": "demo_input/panel_metadata.csv",
                    "description": "Path to the input panel metadata CSV file. This is required.",
                    "help_text": "Path to a CSV file containing panel metadata for the sample."
                },
                "target_bedfile": {
                    "type": "string",
                    "format": "path",
                    "default": "demo_input/targets.bed",
                    "title": "Path to the target BED file",
                    "description": "Path to the BED file defining target regions.",
                    "help_text": "A BED file specifying the genomic target regions for analysis."
                },
                "project_name": {
                    "type": "string",
                    "title": "Project title",
                    "description": "A single project name.",
                    "help_text": "This designates a project name used as the top level of the results directory. Every sample output will be stored in sub-directories under the project name directory."
                },
                "get_sample_report": {
                    "type": "boolean",
                    "default": false,
                    "title": "Generate patient report",
                    "description": "Turn on individual patient sample analysis report generation.",
                    "help_text": "By default the workflow will not generate individual patient sample reports, and instead will only generate the data for use as input to a machine learning classification project. This option turns on the generation of individual patient sample reports based on the final machine learned classifier."
                },
                "client_fields": {
                    "type": "string",
                    "format": "file-path",
                    "description": "A JSON file of key value pairs to display on the report.",
                    "help_text": "This is used to populate a table of additional information to the workflow report.",
                    "hidden": true
                }
            },
            "allOf": [
                {
                    "required": [
                        "project_name",
                        "sample",
                        "bam_directory",
                        "panel_metadata",
                        "target_bedfile"
                    ]
                }
            ]
        },
        "samples": {
            "title": "Sample Options",
            "type": "object",
            "fa_icon": "fas fa-vials",
            "description": "Parameters that relate to samples such as sample sheets and sample names.",
            "properties": {
                "sample_sheet": {
                    "type": "string",
                    "format": "file-path",
                    "description": "A CSV file used to map barcodes to sample aliases.",
                    "help_text": "The sample sheet is a CSV file with, minimally, columns named `barcode` and `alias`. Extra columns are allowed."
                }
            }
        },
        "output": {
            "title": "Output Options",
            "type": "object",
            "fa_icon": "fas fa-arrow-left",
            "description": "Parameters for saving and naming workflow outputs.",
            "properties": {
                "out_dir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "Directory for output of all workflow results. Defaults to <project_name>.results if not set."
                }
            }
        },
        "coverage_options": {
            "title": "Coverage Options",
            "type": "object",
            "fa_icon": "fas fa-chart-bar",
            "description": "Parameters controlling genome coverage thresholds.",
            "properties": {
                "buffersize_bp": {
                    "type": "integer",
                    "default": 2000,
                    "description": "Buffer size in base pairs around target regions.",
                    "help_text": "Defines the buffer added around target regions for analysis."
                },
                "min_genome_coverage": {
                    "type": "number",
                    "default": 0.5,
                    "description": "Minimum acceptable genome coverage fraction.",
                    "help_text": "Samples with coverage below this threshold will be flagged."
                },
                "max_genome_coverage": {
                    "type": "number",
                    "default": 2.0,
                    "description": "Maximum acceptable genome coverage fraction.",
                    "help_text": "Samples with coverage above this threshold will be flagged."
                }
            }
        },
        "advanced_options": {
            "title": "Advanced Options",
            "type": "object",
            "fa_icon": "far fa-question-circle",
            "description": "Advanced options for configuring processes inside the workflow.",
            "help_text": "These advanced options do not need to be changed for typical use, but allow fine tuning of workflows for users who want more control over the workflow.",
            "properties": {
                "max_cores": {
                    "type": "integer",
                    "default": 16,
                    "description": "Maximum number of CPU cores to use across all processes.",
                    "help_text": "Sets an upper bound on the number of cores available to the workflow."
                },
                "wf_humvar_basecaller": {
                    "type": "string",
                    "description": "Basecaller model used for the wf-humvar sub-workflow.",
                    "help_text": "Specify the basecaller model string if required by wf-humvar.",
                    "hidden": true
                },
                "wf_humvar_profile": {
                    "type": "string",
                    "default": "standard",
                    "description": "Profile to use for the wf-humvar sub-workflow.",
                    "help_text": "Controls which wf-humvar profile is applied during variant calling.",
                    "hidden": true
                },
                "wf_humvar_threads": {
                    "type": "integer",
                    "default": 8,
                    "description": "Number of threads for the wf-humvar sub-workflow.",
                    "help_text": "Sets the number of threads passed to the wf-humvar process.",
                    "hidden": true
                },
                "singularity_cache": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "Path to the Singularity image cache directory.",
                    "help_text": "Set this to singularity.cacheDir when using the singularity profile.",
                    "hidden": true
                },
                "cibersortx_username": {
                    "type": "string",
                    "default": "",
                    "description": "CIBERSORTx account username.",
                    "help_text": "Required to authenticate with the CIBERSORTx service.",
                    "hidden": true
                },
                "cibersortx_token": {
                    "type": "string",
                    "default": "",
                    "description": "CIBERSORTx authentication token.",
                    "help_text": "Required to authenticate with the CIBERSORTx service.",
                    "hidden": true
                },
                "r_methyl_container": {
                    "type": "string",
                    "description": "Path or URI to the R methylation/CIBERSORTx container image.",
                    "help_text": "If null, falls back to containers/methylcibersort.sandbox.",
                    "hidden": true
                }
            }
        },
        "misc": {
            "title": "Miscellaneous Options",
            "type": "object",
            "description": "Everything else.",
            "default": "",
            "properties": {
                "disable_ping": {
                    "type": "boolean",
                    "default": false,
                    "description": "Enable to prevent sending a workflow ping."
                },
                "help": {
                    "type": "boolean",
                    "description": "Display help text.",
                    "fa_icon": "fas fa-question-circle",
                    "default": false,
                    "hidden": true
                },
                "version": {
                    "type": "boolean",
                    "description": "Display version and exit.",
                    "fa_icon": "fas fa-question-circle",
                    "default": false,
                    "hidden": true
                }
            }
        }
    },
    "allOf": [
        { "$ref": "#/definitions/input" },
        { "$ref": "#/definitions/samples" },
        { "$ref": "#/definitions/output" },
        { "$ref": "#/definitions/coverage_options" },
        { "$ref": "#/definitions/advanced_options" },
        { "$ref": "#/definitions/misc" }
    ],
    "properties": {
        "aws_image_prefix": {
            "type": "string",
            "hidden": true
        },
        "aws_queue": {
            "type": "string",
            "hidden": true
        },
        "monochrome_logs": {
            "type": "boolean",
            "hidden": true
        },
        "validate_params": {
            "type": "boolean",
            "default": true,
            "hidden": true
        },
        "show_hidden_params": {
            "type": "boolean",
            "hidden": true
        }
    },
    "resources": {
        "recommended": {
            "cpus": 16,
            "memory": "32GB"
        },
        "minimum": {
            "cpus": 2,
            "memory": "4GB"
        },
        "run_time": "Variable per sample",
        "arm_support": false
    }
}
